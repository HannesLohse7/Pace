# lib/supabase

Supabase client setup and typed query helpers.

Intentionally empty — no Supabase integration exists yet. Per `docs/PROJECT_RULES.md`'s MVP scope and this milestone's explicit "no backend, no Supabase" constraint, this folder exists now only so the import path is already the documented convention (`docs/ARCHITECTURE.md`) when that work actually starts.
