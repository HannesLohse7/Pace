# shared/utils

Cross-feature, pure utility functions (e.g. date/duration formatting). No React, no side effects, no feature-specific logic.

Empty as of Milestone 1; scaffolded per `docs/ARCHITECTURE.md`.
