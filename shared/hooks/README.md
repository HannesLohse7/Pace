# shared/hooks

Cross-feature React hooks (e.g. a future `useDebounce`, `useKeyboardHeight`).

Hooks specific to one feature belong in that feature's own `hooks/` folder instead — nothing goes here just because it's a hook.

Empty as of Milestone 1; scaffolded per `docs/ARCHITECTURE.md`.
