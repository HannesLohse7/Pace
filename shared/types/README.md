# shared/types

TypeScript types/interfaces genuinely shared across multiple features (e.g. a future canonical `Workout` model once Training and Workout-detail both need it).

Types used by only one feature belong in that feature's own `types/` folder instead — `features/home/types/home.ts` is the existing example.

Empty as of Milestone 1; scaffolded per `docs/ARCHITECTURE.md`.
